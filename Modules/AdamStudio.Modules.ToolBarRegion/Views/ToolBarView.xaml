<UserControl x:Class="AdamStudio.Modules.ToolBarRegion.Views.ToolBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AdamStudio.Modules.ToolBarRegion.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" 
             xmlns:template="clr-namespace:AdamStudio.Modules.ToolBarRegion.Template"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" 
             xmlns:converters="clr-namespace:AdamStudio.Core.Converters;assembly=AdamStudio.Core"
             xmlns:properties="clr-namespace:AdamStudio.Core.Properties;assembly=AdamStudio.Core" 
             xmlns:controlzex="urn:controlzex"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:in="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:ei="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
             prism:ViewModelLocator.AutoWireViewModel="True" >

    <UserControl.Resources>
        <converters:GridLengthConverter x:Key="GridLengthConverter" />

        <!--#region ResultsTemplate-->

        <ControlTemplate TargetType="ContentControl" x:Key="ResultsTemplate">

            <DockPanel LastChildFill="True">

                <Button DockPanel.Dock="Right"
                        VerticalAlignment="Top"
                        Margin="5 10 5 0"
                        Command="{Binding CleanExecuteEditorDelegateCommand}">

                    <iconPacks:Codicons Kind="ClearAll"/>
                </Button>

                <TabControl Grid.Column="0"
                            Margin="5 10 5 0"
                            BorderThickness="1"
                            TabStripPlacement="Bottom"
                            mah:TabControlHelper.CloseButtonEnabled="False"
                            mah:HeaderedControlHelper.HeaderFontSize="13"
                            mah:TabControlHelper.Underlined="SelectedTabItem"
                            mah:TabControlHelper.UnderlinePlacement="Top"
                            mah:TabControlHelper.UnderlineSelectedBrush="{DynamicResource MahApps.Brushes.Text}"
                            mah:TabControlHelper.UnderlineBrush="{DynamicResource MahApps.Brushes.Text}"
                            BorderBrush="{DynamicResource MahApps.Brushes.Text}">

                    <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.Result}">

                        <ScrollViewer DockPanel.Dock="Right" Name="ResultScrollViewer" VerticalScrollBarVisibility="Auto">

                            <TextBox x:Name="TextResulEditor"
                                     Margin="0 0 0 0"
                                     BorderThickness="0"
                                     HorizontalScrollBarVisibility="Auto"
                                     VerticalScrollBarVisibility="Auto"                 
                                     IsReadOnly="True"
                                     Text="{Binding ResultText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">

                                <in:Interaction.Triggers>
                                    <in:EventTrigger EventName="TextChanged">
                                        <ei:CallMethodAction MethodName="ScrollToEnd" TargetObject="{Binding ElementName=ResultScrollViewer}"/>
                                    </in:EventTrigger>
                                </in:Interaction.Triggers>

                            </TextBox>
                        </ScrollViewer>

                    </mah:MetroTabItem>

                    <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.ExecutionTime}">

                        <StackPanel Margin="5 5 0 0">
                            <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.Succeesed, StringFormat=Succeesed: {0}}" />
                            <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.ExitCode, StringFormat=Exit code: {0} }" />
                            <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.StartTime, StringFormat=Start time: {0:HH:mm.ss.m}}" />
                            <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.EndTime, StringFormat=End time: {0:HH:mm.ss.m}}" />
                            <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.RunTime, StringFormat=Run time (ms): {0} }" />
                            <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.StandardError}"/>
                        </StackPanel>

                    </mah:MetroTabItem>

                    <mah:MetroTabItem Header="Python version">

                        <StackPanel Margin="5 5 0 0">
                            <TextBox BorderThickness="0" Text="{Binding PythonVersion, StringFormat=Python version: {0}, TargetNullValue=Remote source disconected}" />
                            <TextBox BorderThickness="0" Text="{Binding PythonBinPath, StringFormat=Python bin path: {0}, TargetNullValue=''}" />
                            <TextBox BorderThickness="0" Text="{Binding PythonWorkDir, StringFormat=Python work dirrectory: {0}, TargetNullValue=''}" />
                        </StackPanel>

                    </mah:MetroTabItem>

                </TabControl>

            </DockPanel>
        </ControlTemplate>
        
        <!--#endregion-->
        
        <!--#region LogsTemplate-->
        
        <ControlTemplate TargetType="ContentControl" x:Key="LogsTemplate">
            <Grid>
                <TextBlock Text="{Binding ApplicationLogs}"/>
            </Grid>
        </ControlTemplate>

        <!--#endregion-->

    </UserControl.Resources>

    <Grid  x:Name="DockPanel" Height="25">

        <template:PopupWindow Grid.Row="0" DockPanel.Dock="Top"
                              Width="{Binding ActualWidth, ElementName=DockPanel}"
                              Height="{Binding Path=PopupPanelHeight, Mode=TwoWay, Source={x:Static properties:Settings.Default }}"
                              IsOpen="{Binding IsChecked, ElementName=ResultButton, Mode=TwoWay}"
                              PopupWindowTemplate="{StaticResource ResultsTemplate}"/>

        <template:PopupWindow Grid.Row="0" DockPanel.Dock="Top"
                              Width="{Binding ActualWidth, ElementName=DockPanel}"
                              Height="{Binding Path=SSHPanelHeight, Mode=TwoWay, Source={x:Static properties:Settings.Default }}"
                              IsOpen="{Binding IsChecked, ElementName=LogsButton, Mode=TwoWay}"
                              PopupWindowTemplate="{StaticResource LogsTemplate}"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1">

            <ToggleButton x:Name="ResultButton"
                          Content="Результат"  
                          Width="100"
                          FontSize="11"
                          HorizontalAlignment="Left"
                          Style="{StaticResource MahApps.Styles.ToggleButton.Flat}"/>


            <ToggleButton x:Name="LogsButton"
                          Content="Логи"  
                          Width="100"
                          FontSize="11"
                          HorizontalAlignment="Left"
                          Style="{StaticResource MahApps.Styles.ToggleButton.Flat}"/>

        </StackPanel>
    </Grid>


   
    <!--    <DockPanel LastChildFill="False" x:Name="DockPanel">

        <controlzex:PopupEx x:Name="PopupWindow"
                            Height="{Binding Path=PopupPanelHeight, Mode=TwoWay, Source={x:Static properties:Settings.Default }}"
                            MinHeight="200"
                            StaysOpen="True"
                            Placement="Top"
                            CloseOnMouseLeftButtonDown="False"
                            Width="{Binding ActualWidth, ElementName=DockPanel}"
                            IsOpen="{Binding  IsChecked,  ElementName=ResultButton}">

        
                <DockPanel LastChildFill="True" 
                           Background="{DynamicResource MahApps.Brushes.Window.Background}">

                <Thumb DockPanel.Dock="Top"
                       Height="4"
                       BorderThickness="3"
                       BorderBrush="White"
                       Background="{DynamicResource MahApps.Brushes.Text}"
                       Cursor="SizeNS"
                       DragDelta="ThumbDragDelta"/>

                <Border DockPanel.Dock="Top" 
                        BorderThickness="1 0 1 1" BorderBrush="{DynamicResource MahApps.Brushes.Text}">

                    <Grid DockPanel.Dock="Top">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <DockPanel Grid.Column="1">

                            <Button DockPanel.Dock="Top" 
                                    VerticalAlignment="Top"
                                    Margin="5 5 5 0"
                                    Click="CloseButtonClick"
                                    Style="{DynamicResource MahApps.Styles.Button.Chromeless}">
                                
                                <iconPacks:PackIconEvaIcons Kind="Close" Width="10" Height="10"  />

                            </Button>
                        </DockPanel>

                        <TabControl Grid.Column="0"
                                    Margin="5 10 5 0"
                                    BorderThickness="1"
                                    TabStripPlacement="Bottom"
                                    mah:TabControlHelper.CloseButtonEnabled="False"
                                    mah:HeaderedControlHelper.HeaderFontSize="13"
                                    mah:TabControlHelper.Underlined="SelectedTabItem"
                                    mah:TabControlHelper.UnderlinePlacement="Top"
                                    mah:TabControlHelper.UnderlineSelectedBrush="{DynamicResource MahApps.Brushes.Text}"
                                    mah:TabControlHelper.UnderlineBrush="{DynamicResource MahApps.Brushes.Text}"
                                    BorderBrush="{DynamicResource MahApps.Brushes.Text}">

                            <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.Result}">

                                <TextBox x:Name="TextResulEditor"
                                        Margin="0 0 0 0"
                                        BorderThickness="0"
                                        HorizontalScrollBarVisibility="Auto"
                                        VerticalScrollBarVisibility="Auto"                 
                                        IsReadOnly="True"
                                        Text="{Binding ResultText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                            </mah:MetroTabItem>

                            <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.ExecutionTime}">

                                <StackPanel Margin="5 5 0 0">
                                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.Succeesed, StringFormat=Succeesed: {0}}" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.ExitCode, StringFormat=Exit code: {0} }" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.StartTime, StringFormat=Start time: {0:HH:mm.ss.m}}" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.EndTime, StringFormat=End time: {0:HH:mm.ss.m}}" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.RunTime, StringFormat=Run time (ms): {0} }" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.StandardError}"/>
                                </StackPanel>

                            </mah:MetroTabItem>

                            <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.InitilizationTime}">
                                <StackPanel Margin="5 5 0 0">
                                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.Succeesed, StringFormat=Succeesed: {0}}" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.ExitCode, StringFormat=Exit code: {0} }" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.StartTime, StringFormat=Start time: {0:HH:mm.ss.m}}" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.EndTime, StringFormat=End time: {0:HH:mm.ss.m}}" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.RunTime, StringFormat=Run time (ms): {0} }" />
                                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.StandardError}"/>
                                </StackPanel>
                            </mah:MetroTabItem>
                        </TabControl>
                    </Grid>
                </Border>
            </DockPanel>

        </controlzex:PopupEx>

   
        <ToggleButton x:Name="ResultButton"
                      DockPanel.Dock="Left"
                      Content="Результат"  
                      Width="130"
                      Style="{StaticResource MahApps.Styles.ToggleButton.Flat}"
                      />

    </DockPanel>

-->
    <!--    <DockPanel LastChildFill="True">

        <Button DockPanel.Dock="Right"
                VerticalAlignment="Top"
                Margin="5 5 5 0"
                Command="{Binding CleanExecuteEditorDelegateCommand}">
            
            <iconPacks:Codicons Kind="ClearAll"/>
        </Button>


        <mah:MetroTabControl DockPanel.Dock="Top"
                    Margin="0 0 0 0"
                    BorderThickness="0"
                    TabStripPlacement="Bottom"
                    mah:TabControlHelper.CloseButtonEnabled="False"
                    mah:HeaderedControlHelper.HeaderFontSize="13"
                    mah:TabControlHelper.Underlined="SelectedTabItem"
                    mah:TabControlHelper.UnderlinePlacement="Bottom"        
                    mah:TabControlHelper.UnderlineSelectedBrush="{DynamicResource MahApps.Brushes.Text}"
                    mah:TabControlHelper.UnderlineBrush="{DynamicResource MahApps.Brushes.Text}"
                    BorderBrush="{DynamicResource MahApps.Brushes.Text}">

            <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.Result}">

                <TextBox x:Name="TextResulEditor"
                                     Margin="0 0 0 0"
                                     BorderThickness="0"
                                     HorizontalScrollBarVisibility="Auto"
                                     VerticalScrollBarVisibility="Auto"                 
                                     IsReadOnly="True"
                                     Text="{Binding ResultText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            </mah:MetroTabItem>

            <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.ExecutionTime}">
                <StackPanel Margin="5 5 0 0">
                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.Succeesed, StringFormat=Succeesed: {0}}" />
                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.ExitCode, StringFormat=Exit code: {0} }" />
                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.StartTime, StringFormat=Start time: {0:HH:mm.ss.m}}" />
                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.EndTime, StringFormat=End time: {0:HH:mm.ss.m}}" />
                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.RunTime, StringFormat=Run time (ms): {0} }" />

                    <TextBox BorderThickness="0" Text="{Binding ResultExecutionTime.StandardError}"/>
                </StackPanel>

            </mah:MetroTabItem>

            <mah:MetroTabItem Header="{DynamicResource ScratchControlView.MetroTabItem.Header.InitilizationTime}">
                <StackPanel Margin="5 5 0 0">
                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.Succeesed, StringFormat=Succeesed: {0}}" />
                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.ExitCode, StringFormat=Exit code: {0} }" />
                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.StartTime, StringFormat=Start time: {0:HH:mm.ss.m}}" />
                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.EndTime, StringFormat=End time: {0:HH:mm.ss.m}}" />
                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.RunTime, StringFormat=Run time (ms): {0} }" />

                    <TextBox BorderThickness="0" Text="{Binding ResultInitilizationTime.StandardError}"/>

                </StackPanel>
            </mah:MetroTabItem>

        </mah:MetroTabControl>
    </DockPanel>-->
</UserControl>
